# ===================================
#  Form Field Definitions
# ===================================

fields:

    # Hide this field for context 'relation'
    order:
        type: relation
        nameFrom: id
        emptyOption: -- Select Order --
        comment: This Order Detail belongs to the Order selected above.
        context: ['create', 'update']

    first_name:
        label: first_name
        searchable: true

    last_name:
        label: last_name
        searchable: true

    email:
        label: email
        searchable: true

    mobile:
        label: mobile
        searchable: true

    telphone:
        label: telphone
        searchable: true

    company:
        label: company
        searchable: true

    title:
        label: title

    country:
        label: country

    address:
        label: address

    zip:
        label: zip

    photo:
        label: photo