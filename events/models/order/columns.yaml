# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    total_amount:
        label: total_amount

    real_amount:
        label: real_amount

    discount_amount:
        label: discount_amount

    paid_amount:
        label: paid_amount

    trade_no:
        label: trade_no

    pay_code:
        label: pay_code

    badge_code:
        label: badge_code

    status:
        label: status
        type: switch

    pay_status:
        label: pay_status
        type: switch