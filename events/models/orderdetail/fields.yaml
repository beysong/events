# ===================================
#  Form Field Definitions
# ===================================

fields:

    # Hide this field for context 'relation'
    order:
        type: relation
        nameFrom: name
        emptyOption: -- Select Event --
        comment: This Ticket belongs to the Event selected above.
        context: ['create', 'update']

    first_name:
        label: first_name
        searchable: true

    last_name:
        label: last_name
        searchable: true

    email:
        label: email

    mobile:
        label: mobile

    telphone:
        label: telphone

    company:
        label: company

    title:
        label: title

    country:
        label: country

    address:
        label: address

    zip:
        label: zip

    photo:
        label: photo